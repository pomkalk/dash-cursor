# DocumentsHeader

- **Path:** `src/components/DocumentsHeader/index.tsx`
- **Role:** заголовок страницы `Documents` + primary CTA.

## Props
- `title: string`.
- `onCreateDocument: () => void`.

## Behavior
- Кнопка `+ New Document` триггерит создание новой записи через модалку.
