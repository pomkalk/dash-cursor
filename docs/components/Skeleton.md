# Skeleton

- **Path:** `src/components/Skeleton/index.tsx`
- **Role:** placeholder для loading-состояний.

## Props
- `className?: string` — задает размер/форму skeleton-блока.

## Behavior
- Использует `animate-pulse`.
- Активно применяется в `RecentFilesTable` при `isLoading`.
